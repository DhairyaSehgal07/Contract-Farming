import { RoleStubPage } from "@/components/role-stub-page";

export default function ManagerReportsPage() {
  return (
    <RoleStubPage
      title="Reports"
      description="Review per-farmer PDF report cards and lifecycle summaries."
      body="Use reports to validate data quality and support operational decisions with admins."
    />
  );
}
