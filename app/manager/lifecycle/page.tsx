import { RoleStubPage } from "@/components/role-stub-page";

export default function ManagerLifecyclePage() {
  return (
    <RoleStubPage
      title="Lifecycle"
      description="Review irrigation, roguing, strip tests, and dehalming activity across lands."
      body="Provide instructions where the PRD calls for admin or manager review on irrigation and visits."
    />
  );
}
