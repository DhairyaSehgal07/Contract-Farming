import { RoleStubPage } from "@/components/role-stub-page";

export default function ManagerLandsPage() {
  return (
    <RoleStubPage
      title="Lands"
      description="Track lands, areas, and lifecycle progress for operational oversight."
      body="Drill into plantation through final visit data to support decisions in the field."
    />
  );
}
