import { RoleStubPage } from "@/components/role-stub-page";

export default function StaffSettingsPage() {
  return (
    <RoleStubPage
      title="Settings"
      description="Account and device preferences for working offline-first when supported."
      body="Tune notifications and defaults so reminders and capture fit your field routine."
    />
  );
}
