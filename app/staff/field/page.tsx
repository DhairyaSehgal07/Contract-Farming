import { RoleStubPage } from "@/components/role-stub-page";

export default function StaffFieldPage() {
  return (
    <RoleStubPage
      title="Field work"
      description="Upload photos, videos, and notes for irrigation, roguing, strip tests, and visits."
      body="Structured capture per land and stage, with timelines managers and admins can review."
    />
  );
}
