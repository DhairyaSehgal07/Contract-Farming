import { RoleStubPage } from "@/components/role-stub-page";

export default function StaffMessagesPage() {
  return (
    <RoleStubPage
      title="Messages"
      description="Respond to admins and align with managers on tasks and clarifications."
      body="As in the PRD: admins message staff; staff coordinate back from the field."
    />
  );
}
