import { RoleStubPage } from "@/components/role-stub-page";

export default function StaffRemindersPage() {
  return (
    <RoleStubPage
      title="Reminders"
      description="Tasks driven by plantation dates: visits, roguing, strip tests, and follow-ups."
      body="Your checklist for the field—synced with the organization reminder engine when live."
    />
  );
}
