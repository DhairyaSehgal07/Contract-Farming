import { RoleStubPage } from "@/components/role-stub-page";

export default function StaffLandsPage() {
  return (
    <RoleStubPage
      title="Lands"
      description="Lands assigned to you with area and lifecycle context for each visit."
      body="Jump from here into field work and reminders for the right plot at the right time."
    />
  );
}
