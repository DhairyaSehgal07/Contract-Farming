import { RoleStubPage } from "@/components/role-stub-page";

export default function AdminRemindersPage() {
  return (
    <RoleStubPage
      title="Reminders"
      description="Automatic reminders from plantation dates for visits, roguing, strip tests, and follow-ups."
      body="You will see due and upcoming tasks here, tuned to your organization’s schedule."
    />
  );
}
