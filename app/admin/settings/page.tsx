import { RoleStubPage } from "@/components/role-stub-page";

export default function AdminSettingsPage() {
  return (
    <RoleStubPage
      title="Settings"
      description="Organization preferences, integrations, and notification defaults."
      body="Configure how reminders, reports, and communications behave for your org."
    />
  );
}
