import { RoleStubPage } from "@/components/role-stub-page";

export default function AdminLifecyclePage() {
  return (
    <RoleStubPage
      title="Lifecycle"
      description="Plantation through final visit: irrigation, roguing, strip tests, and dehalming."
      body="Structured stages per land with media uploads, observations, and admin or manager review."
    />
  );
}
