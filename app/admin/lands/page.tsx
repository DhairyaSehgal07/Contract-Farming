import { RoleStubPage } from "@/components/role-stub-page";

export default function AdminLandsPage() {
  return (
    <RoleStubPage
      title="Lands"
      description="Register multiple lands per farmer with name, area, and optional geo-location."
      body="Land detail pages will host plantation entries, irrigation timelines, and visit reporting."
    />
  );
}
